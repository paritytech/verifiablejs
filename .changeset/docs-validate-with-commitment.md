---
"verifiablejs": patch
---

Docs: steer verification guidance toward `validate_with_commitment` — fetch the finalized 288-byte `MembersCommitment` from chain instead of rebuilding it from the member list on every `validate` call.
